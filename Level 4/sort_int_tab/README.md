## Subject

```
Assignment name  : sort_int_tab
Expected files   : sort_int_tab.c
Allowed functions:
--------------------------------------------------------------------------------

Escreva a seguinte função:

void sort_int_tab(int *tab, unsigned int size);

Ela deve classificar (no local) o array 'tab' int, que contém exatamente os membros 'size'
, em ordem crescente.

Os doubles devem ser preservados.

A entrada é sempre coerente.
```
